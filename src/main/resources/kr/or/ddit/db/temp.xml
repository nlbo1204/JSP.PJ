<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="temp">
  <select id="temp" resultType="map">
  <!-- 
  	alias 사용 주의점 
  	  - alias를 ""안에 사용하지 않을 시 소문자로 기재를 해도 대문자로 인식하게 된다.
  	  - 소문자로 인식을 하게 할 경우 ""안에  alias를 사용한다.	
  -->
    select 'X' as "result" from dual
  </select>
</mapper>